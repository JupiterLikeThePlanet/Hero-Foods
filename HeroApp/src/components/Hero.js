import React from 'react';
import ReactDOM from 'react-dom';

const Hero = (props) => (

            <div >

                <h5> hero || food </h5>

            </div>

        );

export default Hero